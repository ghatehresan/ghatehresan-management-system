<?php
/**
 * تنظیمات برنامهٔ مدیریت قطعه‌رسان
 * ------------------------------------------------------------------
 * پیش‌فرض روی SQLite است تا بدون هیچ تنظیماتی کار کند.
 * اگر می‌خواهید از MySQL ایکس‌امپ استفاده کنید:
 *   ۱) در phpMyAdmin یک دیتابیس به نام ghatehresan بسازید
 *      با Collation = utf8mb4_persian_ci
 *   ۲) مقدار driver را به 'mysql' تغییر دهید
 * ------------------------------------------------------------------
 */

return [

    'db' => [

        // 'sqlite'  → بدون نیاز به تنظیمات (توصیه‌شده برای شروع)
        // 'mysql'   → استفاده از MySQL ایکس‌امپ
        'driver' => 'sqlite',

        'sqlite' => [
            'path' => dirname(__DIR__) . '/storage/ghatehresan.sqlite',
        ],

        'mysql' => [
            'host'     => '127.0.0.1',
            'port'     => 3306,
            'database' => 'ghatehresan',
            'username' => 'root',
            'password' => '',
            'charset'  => 'utf8mb4',
        ],
    ],

    'app' => [
        'name'      => 'قطعه‌رسان',
        'title'     => 'سامانهٔ مدیریت قطعه‌رسان',
        'timezone'  => 'Asia/Tehran',

        // در حالت توسعه true کنید تا خطاها نمایش داده شوند
        'debug'     => false,

        // مسیر پوشهٔ ذخیره‌سازی (دیتابیس، پشتیبان، فایل‌ها)
        'storage'   => dirname(__DIR__) . '/storage',
    ],

    'defaults' => [
        // کارمزد درگاه پرداخت بر حسب درصد — در تنظیمات قابل تغییر است
        'gateway_fee_percent' => 0,
        // هزینهٔ پیش‌فرض بسته‌بندی هر سفارش (تومان)
        'packaging_cost'      => 0,
        // آستانهٔ پیش‌فرض تصمیم انبار: حداقل فروش در ۹۰ روز
        'stock_min_sales_90d' => 3,
    ],
];
